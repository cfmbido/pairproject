<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Deadline</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% for (let i = 0; i < data.length; i++) { %>
            <tr>
                <td><%= data[i].name %></td>
                <td><%= data[i].deadline %></td>
                <td><a href="/project/edit/<%= data[i].id %>">Edit</a> | <a href="/project/delete/<%= data[i].id %>">Delete</a></td>
            </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>